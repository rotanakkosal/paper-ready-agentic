
services:
  qdrant:
    image: qdrant/qdrant:v1.11.0
    container_name: paper-ready-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"   # HTTP API + dashboard
      - "6334:6334"   # gRPC 
    volumes:
      - qdrant_storage:/qdrant/storage
    environment:
      - QDRANT__SERVICE__GRPC_PORT=6334
   
volumes:
  qdrant_storage:
