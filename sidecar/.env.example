# PaperReady sidecar configuration.
# All values are optional — the sidecar runs with sensible defaults if unset.

# Override the path to the CSV data directory.
# Default: ../ingest/out/ resolved relative to this file.
# PAPERREADY_DATA_DIR=

# Port for uvicorn (default 8000).
# PORT=8000
